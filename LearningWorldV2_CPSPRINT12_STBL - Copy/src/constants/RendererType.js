/**
 * Renderer type names.
 */
export const RendererType = Object.freeze({
  SELECT: 'select',
  IMAGE_SELECT: 'image-select',
  DRAG: 'drag',
  MATCH: 'match',
  ORDER: 'order',
  CATEGORIZE: 'categorize',
});
